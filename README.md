# ML learning repo

*learning... in public?!*

for initialization on new device, once in directory:

macOS/Linux
```
python -m venv .venv
source .venv/bin/activate
python -m pip install -r requirements.txt
```

Windows PowerShell
```
python -m venv .venv
.venv\Scripts\Activate.ps1
python -m pip install -r requirements.txt
```

